// TestBed environment is initialized per-file via import '../test-env'
// (Required because the Analog AOT plugin isolates module scopes per file,
// so setupFile state does not propagate to test files)
